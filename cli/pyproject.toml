[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pauk"
version = "0.16.0"
description = "CLI client for the pauk flashcard app"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.12",
    "httpx>=0.27",
    "rich>=13.7",
    "pyyaml>=6.0",
    "textual>=8.0",
    "textual-image>=0.8",
    "pillow>=10.0",
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pexpect>=4.9", "pytest-asyncio>=0.23"]
# optional in-process local LLM runtime (llama.cpp): CPU on Linux,
# Metal on Apple silicon; prebuilt wheels exist. Keeps the base
# install light — `pip install 'pauk[local]'` to run quests/tips
# without the `claude` CLI.
local = ["llama-cpp-python>=0.3"]

[project.scripts]
pauk = "pauk.main:run"

[tool.setuptools.packages.find]
include = ["pauk*"]

[tool.setuptools.package-data]
"pauk.tui" = ["*.tcss", "assets/*.png"]
"pauk.data" = ["*.json"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
