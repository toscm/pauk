# Serve real files (media/) directly; everything else goes to
# the API front controller. FallbackResource instead of
# mod_rewrite: IONOS's Apache 500s on per-directory rewrites.
FallbackResource /index.php
